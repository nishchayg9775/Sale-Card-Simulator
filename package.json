{
  "name": "sale-card-simulator",
  "private": true,
  "version": "1.0.0",
  "description": "Univest Card Studio with shared server-backed library storage",
  "scripts": {
    "start": "node server.js",
    "build:pages": "node scripts/build-pages.js",
    "check": "node --check app-config.js && node --check app.js && node --check server.js && node --check scripts/build-pages.js && node --check scripts/run-qa.js",
    "verify": "npm run check && npm run build:pages",
    "qa": "node scripts/run-qa.js regression",
    "qa:all": "node scripts/run-qa.js all",
    "qa:feature": "node scripts/run-qa.js feature",
    "qa:folder": "node scripts/run-qa.js folder",
    "qa:offer": "node scripts/run-qa.js offer",
    "qa:transform": "node scripts/run-qa.js transform"
  },
  "dependencies": {
    "@google/genai": "^1.48.0"
  }
}
